import React, { Component } from 'react';
import {
  Text,
  View,
  Image,
  Dimensions,
  StatusBar,
  TouchableHighlight,
  MapView,
} from 'react-native';

import BottomNavBar from './BottomNavBar';

const Home = ({ navigator }) => {

  return (
    <View>

    </View>
  )
};

// stylin'!
const styles = {

}


export default Home;
